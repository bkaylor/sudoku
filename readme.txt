A sudoku solver in c/c++.
Takes one input, a filename containing a sudoku puzzle.
Examples provided.

build.bat builds for Windows
build.sh builds for Mac/Linux

It's worth noting that build.bat calls clang, while build.sh calls gcc.
Also worth noting that while working on this project, I learned that gcc on macOS is an alias for clang.
